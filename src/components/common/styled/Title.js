import styled from 'styled-components';

export const Title = styled.h3`
  text-align: left;
  color: white;
  font-size: 32px;
  font-weight: 800;
  margin: 0 0 0 0;
  font-family: sans-serif;
  letter-spacing: -1px;
`;
